# Vendor Compiler Engine

This directory contains the current compiler engine snapshot required by runtime tests and compiled runtime configuration paths.

Vendor documentation was reduced in the v34.3 GitHub-ready package. Keep code and machine-readable assets intact; maintain detailed compiler documentation in the compiler repository.
