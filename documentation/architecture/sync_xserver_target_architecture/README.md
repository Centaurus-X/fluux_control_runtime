# Sync XServer Target Architecture

This directory contains the preserved target architecture diagram artifacts that were previously stored in `product/`.

Files:

```text
sync_xserver_target_architecture.mmd   Mermaid source
sync_xserver_target_architecture.svg   Rendered SVG artifact
sync_xserver_target_architecture.html  HTML/Mermaid viewer artifact
```

These files are documentation artifacts and are intentionally outside the runtime product source tree.
